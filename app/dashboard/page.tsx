import { MetricCard } from '@/components/metric-card';
import { WorkQueue } from '@/components/work-queue';
import { buildDashboardSnapshot } from '@/lib/reporting/dashboard';
import { DEMO_COMPLAINTS, DEMO_DATA_NOTICE, DEMO_RECOMMENDATIONS } from '@/lib/reporting/demo-data';
export default function DashboardPage(){ const snapshot=buildDashboardSnapshot(DEMO_COMPLAINTS,DEMO_RECOMMENDATIONS,new Date('2026-08-31T00:00:00Z')); return <><header className="oc-page-head"><div><h1>Executive Overview</h1><p>Release 1 oversight dashboard for complaints, investigations, Leadership matters and recommendation compliance.</p></div><span className="oc-badge">DEMO MODE</span></header><div className="oc-notice">{DEMO_DATA_NOTICE}</div><div className="oc-grid"><MetricCard label="Complaints" value={snapshot.complaintsTotal}/><MetricCard label="Open matters" value={snapshot.complaintsOpen}/><MetricCard label="Overdue matters" value={snapshot.complaintsOverdue}/><MetricCard label="Recommendation implementation" value={`${snapshot.recommendations.implementationRate}%`}/></div><div className="oc-columns"><WorkQueue items={[{title:'Complaint screening',detail:'Review newly registered DEMO matters.'},{title:'Leadership review',detail:`${snapshot.leadershipMatters} restricted DEMO matter in workflow.`},{title:'Compliance follow-up',detail:`${snapshot.recommendations.overdue} DEMO recommendation overdue.`}]}/><div className="oc-card"><h3>Age profile — open complaints</h3><ul className="oc-list"><li>Under 30 days: {snapshot.ageBands.under30}</li><li>30–89 days: {snapshot.ageBands.days30to89}</li><li>90+ days: {snapshot.ageBands.days90plus}</li></ul></div></div></>; }
