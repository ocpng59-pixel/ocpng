'use client';
import { useState } from 'react'; import Link from 'next/link'; import { createBrowserSupabaseClient } from '@/lib/supabase/browser';
export default function ForgotPasswordPage(){ const client=createBrowserSupabaseClient(); const [email,setEmail]=useState(''); const [message,setMessage]=useState(''); async function submit(e:React.FormEvent){e.preventDefault(); if(!client)return; const {error}=await client.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/set-password`}); setMessage(error?error.message:'If the account exists, a secure recovery link has been sent.');} return <main className="oc-auth-wrap"><section className="oc-auth-card"><h1>Password recovery</h1><p className="oc-muted">Request a secure recovery link for your authorised OCPNG account.</p>{!client?<div className="oc-notice">Authentication is not configured in this environment.</div>:null}<form className="oc-form" onSubmit={submit}><div><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="oc-button" disabled={!client}>Send recovery link</button></form>{message?<p className="oc-muted">{message}</p>:null}<p className="oc-muted"><Link href="/login">Return to sign in</Link></p></section></main>; }
